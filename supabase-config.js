/*
 * Runtime configuration. Replace these placeholders in deployment settings.
 * Never commit a service-role key; the anon key is safe for browser use when
 * protected by Supabase Row Level Security policies.
 */
window.DANGOTE_SUPABASE_CONFIG = Object.freeze({
  url: '',
  anonKey: ''
});
